"""
Aegis Engine — clean rebuild.

An honest, rules-based intraday breakout engine. No neural nets, no black boxes.
The whole point: prove (or disprove) a real trading edge on OUT-OF-SAMPLE data,
with realistic costs and no look-ahead, before a single dollar is risked.

See the plan: ../market_agent is the OLD system (frozen, negative edge). This
package is the fresh start and does NOT import from market_agent.
"""
__version__ = "0.1.0"
